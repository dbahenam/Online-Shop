<!DOCTYPE html>
<html lang="en">
  <%- include("../../shared/includes/head") %>
  <link rel="stylesheet" href="/styles/details.css" />
  <script src="/scripts/cartBadge.js" defer></script>
  <body>
    <header><%- include("../../shared/includes/header") %></header>
    <main>
      <div class="container">
        <div class="row">
          <div class="col-md-5">
            <img
              src="<%=product.imageUrl%>"
              alt="<%=product.name%>"
              class="img-thumbnail"
            />
          </div>
          <div class="col-md-5">
            <p class="newarrival text-center">New</p>
            <h2><%=product.name%></h2>
            <p class="price">USD $<%=product.price%></p>
            <p><b>Availability: </b>In stock</p>
            <p><b>Description: </b><%=product.description%></p>
            <p><b>Condition: </b>New</p>

            <label for="item-quantity"><b>Quantity:</b></label>
            <input
              id="item-quantity"
              name="item-quantity"
              type="number"
              value="1"
              min="1"
              max="10"
            />
            <br />
            <button
              class="btn btn-warning"
              role="button"
              id="addToCart"
              data-productid="<%=product.id%>"
              data-csrf="<%= locals.csrfToken %>"
            >
              Add to Cart
            </button>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
